# NLP Lab

This lab focuses on text preprocessing and regular expressions using Python.

## Lab Tasks

* Extracting and counting hashtags from the Apple Twitter dataset.
* Finding the top 10 most frequently used hashtags.
* Using `re.compile()` to create a regular expression pattern.
* Using `re.split()` to split text based on digits.
* Tokenizing text using spaCy and NLTK.
